/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['https://cdn-icons-png.flaticon.com', 'cdn-icons-png.flaticon.com']
  }
}

module.exports = nextConfig
